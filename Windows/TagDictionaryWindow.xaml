<local:FilterizerWindow x:Class="Filterizer2.Windows.TagDictionaryWindow"
                              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                              xmlns:local="clr-namespace:Filterizer2"
                              xmlns:windows="clr-namespace:Filterizer2.Windows"
                              Title="Tag Dictionary" Height="450" Width="800">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Left side: List of tags and filter box -->
        <Grid Grid.Column="0" Grid.Row="0" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            
            <TextBox Grid.Row="0" Name="TagFilterTextBox" Width="200" Margin="0,0,0,10" TextChanged="TagFilterTextBox_TextChanged"/>

            <!-- <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" BorderThickness="1" BorderBrush="DarkGray"> -->
                <ListBox Name="TagsListBox" SelectionMode="Single" MinHeight="200" BorderThickness="1" Grid.Row="1" BorderBrush="DarkGray"
                         SelectionChanged="TagsListBox_SelectionChanged">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{Binding DisplayColorBrush}"/>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            <!-- </ScrollViewer> -->
        </Grid>

        <!-- Right side: Tag details -->
        <StackPanel Grid.Column="1" Grid.Row="0" Margin="10">
            <windows:TagDetailsControl x:Name="TagDetails"/>
            <StackPanel Orientation="Horizontal">
                <Button Content="Create new tag" Name="NewTagButton" Margin="20,5,10,0" Click="NewTagButton_Click"/>
                <Button Content="Edit selected tag" Name="EditTagButton" IsEnabled="False" Margin="20,5,10,0" Click="EditTagButton_Click"/>
                <Button Content="Delete selected tag" Name="DeleteTagButton" IsEnabled="False" Margin="20,5,10,0" Click="DeleteTagButton_Click"/>
            </StackPanel>
            <Border x:Name="TagNameHierarchyPanel" BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="0,5,0,0" CornerRadius="5" Visibility="Hidden">
                <TreeView x:Name="TagNameHierarchy">
                    <TreeView.ItemContainerStyle>
                        <Style TargetType="{x:Type TreeViewItem}">
                            <Style.Triggers>
                                <Trigger Property="HasItems" Value="True">
                                    <Setter Property="Focusable" Value="False" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TreeView.ItemContainerStyle>
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate DataType="{x:Type local:TagItem}" ItemsSource="{Binding ImmediateParentTags}">
                            <TextBlock Text="{Binding NamesAndAliasesAsString}" />
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </Border>
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal">
            <!-- Bottom: Import tag dictionary button and open hierarchy view-->
            <Button Content="Import or export tag dictionary." Click="IOTagDictionary_OnClick"/>
            <Button Margin="2, 0, 0, 0" Content="Tag Hierarchy" Click="ButtonBase_OnClick"/>
        </StackPanel>
    </Grid>
</local:FilterizerWindow>